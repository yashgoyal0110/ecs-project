apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-data
  namespace: products
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # kind ships with the "standard" StorageClass (rancher.io/local-path) by default.
